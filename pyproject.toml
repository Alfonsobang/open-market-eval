[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "open-market-eval"
version = "0.5.0"
description = "A-Share Agent Arena for reproducible backtest forensics and research evaluation"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}
authors = [{name = "OpenMarketEval contributors"}]
keywords = ["a-share", "finance", "agent-evaluation", "backtest", "harbor", "llm", "benchmark"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "License :: OSI Approved :: Apache Software License",
  "Programming Language :: Python :: 3",
  "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.scripts]
open-market-eval = "open_market_eval.cli:main"

[tool.setuptools]
packages = ["open_market_eval"]

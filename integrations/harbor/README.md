# Harbor integration

`market-forecast/` is a small Harbor-style task that evaluates schema compliance, evidence cutoff discipline, and safe framing with a deterministic verifier.

It is designed as an integration example for [harbor-framework/harbor](https://github.com/harbor-framework/harbor), not as a claim of official Harbor compatibility or endorsement. The task uses synthetic fixtures and runs without network access.
